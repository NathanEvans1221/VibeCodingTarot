# VibeCodingTarot 架構文檔

## 系統架構概覽

```
┌─────────────────────────────────────────────────────────┐
│                     用戶瀏覽器                           │
│  (HTML5 + CSS3 + JavaScript ES6+)                      │
└─────────────────────┬───────────────────────────────────┘
                      │ HTTP/HTTPS
                      ▼
┌─────────────────────────────────────────────────────────┐
│                   Flask 應用伺服器                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │   路由層     │  │   邏輯層     │  │   資料層     │     │
│  │  (Routes)   │  │ (Services)  │  │  (Models)   │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│                   資料儲存層                              │
│  ┌─────────────────────────────────────────────────┐   │
│  │         tarot_cards.json (JSON 檔案)             │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

## 核心設計決策

### 1. 無資料庫架構 (D1)
- **決策**: 使用 JSON 檔案而非 SQLite/PostgreSQL
- **理由**: 
  - 塔羅牌資料為靜態資料，無需複雜查詢
  - 簡化部署，無需資料庫服務
  - 易於版本控制和備份
- **權衡**: 犧牲查詢效能與並發處理能力

### 2. 單頁模板模式 (D2)
- **決策**: 每種占卜類型獨立 HTML 模板
- **理由**: 
  - 簡化開發，降低耦合度
  - 易於維護和擴展
- **已知問題**: 模板間存在重複代碼（header/footer/JS）

### 3. 前端渲染模式 (D3)
- **決策**: 使用 JavaScript 在前端處理占卜邏輯
- **理由**: 
  - 減輕伺服器負擔
  - 提升用戶體驗（即時回應）
- **限制**: 無法進行伺服器端驗證

## 目錄結構

```
VibeCodingTarot/
├── app.py                    # Flask 主應用入口
├── requirements.txt          # Python 依賴清單
├── data/
│   └── tarot_cards.json     # 塔羅牌資料 (22 Major Arcana)
├── templates/
│   ├── index.html           # 首頁
│   ├── single_card.html     # 單張牌占卜頁面
│   └── three_cards.html     # 三張牌占卜頁面
├── static/
│   ├── css/
│   │   └── style.css        # 全域樣式
│   └── js/
│       └── main.js          # 全域 JavaScript 邏輯
├── docs/
│   ├── API.md               # API 詳細文檔
│   └── DECISIONS.md         # 架構決策紀錄
├── CLAUDE.md                # Claude Code 指令
├── AGENTS.md                # OpenCode Agent 協調
├── GEMINI.md                # Google Gemini 指令
├── ARCHITECTURE.md          # 本文件
├── CONTRIBUTING.md          # 貢獻指南
├── CHANGELOG.md             # 變更日誌
├── PRD.md                   # 產品需求文檔
└── README.md                # 專案說明
```

## 資料流

### 單張牌占卜流程
```
用戶輸入問題 → POST /api/draw-single
    ↓
後端隨機選牌 → 從 tarot_cards.json 讀取
    ↓
返回牌面資料 → 前端渲染結果
    ↓
顯示正位/逆位含義
```

### 三張牌占卜流程
```
用戶輸入問題 → POST /api/draw-three
    ↓
後端隨機選3張牌 → 分別代表 過去/現在/未來
    ↓
返回牌面資料陣列 → 前端依序渲染
    ↓
顯示時間軸解讀
```

## API 設計原則

1. **RESTful 風格**: 使用標準 HTTP 方法
2. **JSON 回應格式**: 統一 `{"success": bool, "data": {...}, "message": "string"}`
3. **無狀態設計**: 每次請求獨立處理
4. **錯誤處理**: 統一錯誤回應格式

## 擴展性考量

### 短期 (v0.2)
- [ ] 實作 56 張 Minor Arcana
- [ ] 新增凱爾特十字牌陣
- [ ] 占卜記錄持久化

### 中期 (v0.5)
- [ ] 用戶帳號系統
- [ ] 占卜歷史查詢
- [ ] 社群分享功能

### 長期 (v1.0)
- [ ] 多語言支援
- [ ] AI 解讀整合
- [ ] 行動應用

## 效能考量

- **快取策略**: JSON 檔案可快取於記憶體
- **壓縮**: 靜態資源使用 gzip 壓縮
- **CDN**: 圖片資源可使用 CDN 加速

## 安全性考量

- **CSRF 保護**: 已實作 CSRF token
- **XSS 防護**: 輸入內容已過濾
- **SECRET_KEY**: 使用環境變數，不硬編碼
